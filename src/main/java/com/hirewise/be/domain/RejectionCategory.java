package com.hirewise.be.domain;

/** Broad grouping of a {@link RejectionReason} for reporting (BR-REJ-01). */
public enum RejectionCategory {
    SKILL_GAP,
    CULTURE_GAP,
    SALARY_GAP,
    DUPLICATE,
    WITHDRAWN,
    OTHER
}
